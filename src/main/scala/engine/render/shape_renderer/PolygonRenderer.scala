// package engine.render.shape_renderer

// import engine.math.Vector2
// import engine.math.shapes.Polygon

/* Note: Consider creating constructor that takes a FloatBuffer and IntBuffer for optional optimization.
 */

// case class PolygonRenderer(points: Vector[Vector2]) extends ShapeRenderer {
// }

// object PolygonRenderer {
//   def apply(polygon: Polygon): PolygonRenderer = {
//     PolygonRenderer(polygon.points)
//   }
// }
